# 工作歷程

## 2026-09-05：私人儲存庫與第 0 階段

- 目標：依授權建立私人 GitHub 儲存庫，執行案例盤點與方法萃取；繁體中文優先。
- 儲存庫：建立 `wicanr2/ai-software-archaeology-handbook`，建立後以 `gh repo view` 回讀確認 `PRIVATE`；本機初始化 `main` 與 `origin`。
- 認證：既有 GitHub CLI 容器未取得主機 keyring，兩次讀取回 HTTP 401。使用者指出主機有 `gh` 後，改用主機既有認證完成已授權操作；未搬移或輸出明文憑證。
- 隔離：Docker socket 的沙箱存取曾遭拒，經提升權限後正常執行。案例分析均以有資源限制、外層逾時、`--rm`、唯讀掛載及目前使用者 UID/GID 的容器進行；未修改案例來源。
- 變更：新增四份 `docs/` 研究文件；在 AGENTS／PLAN 記錄繁體中文先行、最後擴充英文與日文的指示。本檔依共用文件職責規則獨立記錄作業歷程，不把流水帳加進 README。
- 證據：七個完整提交、二十筆來源文件 SHA-256；已閱讀文件及 OnePCE 的測試／擷取腳本、Parhelion 的差分程式碼，整理七個重複模式與八個歷史失敗案例。
- 新發現：MM2 格式文件與現況勘誤仍有衝突；OnePCE 畫面測試可略過缺少的記憶體檔，需在方法中區分缺失觀測與相符。
- 驗證：二十筆來源雜湊已直接對固定提交 Git 物件核對一致；另核對連結、文件結構、Git 差異與擁有權。未執行原版、案例測試或產品建置。
- 檢查腳本訂正：首次用來源工作樹核對時，Parhelion 兩筆不符；確認它在本輪已有新提交，因此改查最初固定 Git 物件，全數吻合。模式數量檢查也因未錨定列首而多算引用列，修正後重查；兩者均屬驗證方法問題，沒有改寫來源證據。
- 未完成：第 0 階段仍待使用者審查；未開始第 1 階段、schema、CLI 或真實介接器。
- 首次提交檢查：AGENTS／PLAN 原有兩處 Markdown 行尾空白使 `git diff --cached --check` 失敗，移除後通過。新儲存庫未設定作者，核對既有專案後僅在本儲存庫沿用相同姓名與信箱，未變更全域設定。
- 提交與推送：`f32b37d`（第 0 階段案例盤點與方法草案）已推送至私人儲存庫 `main`；本筆收尾另作文件提交。
- 最終檢查：七份文件的連結、圍欄、擁有權與本輪新增研究文字的繁體用字檢查通過；模式列七項、失敗案例八項。二十筆來源雜湊通過固定提交驗證；沒有執行或宣稱原版相容性測試。
- Docker 清理：以 `osa.task=phase0` 標籤查詢執行中及已停止容器，結果為零；本輪沒有建立映像檔或留下容器產物。

## 2026-09-05：完成繁體中文手冊及英日版本

- 授權：第 0 階段交付後，專案負責人要求「完成手冊」。本輪將此視為第 1 階段及先前語言安排的繼續授權，不推定第 2–7 階段工具工作已完成或已獲實作授權。
- 起點：重新核對乾淨工作樹、既有提交與四份研究文件。來源以固定 Git 物件為準，不重開案例專案的歷史待辦。
- 撰寫順序：先完成繁體中文九章、目錄及人工累加器演練，審讀內容並核對九筆手算結果，再完成相同內容的英文與日文。
- 交付：`docs/handbook/`、`docs/handbook/en/`、`docs/handbook/ja/` 各九章、目錄及完整演練，共 33 頁；根 README 提供閱讀入口，`docs/handbook-review.md` 保存逐需求驗收。
- 現況同步：AGENTS、PLAN 與 inventory 記錄本輪授權及手冊範圍；第 0 階段草案保留原文，新增歷史狀態提示；沒有改寫舊工作歷程。
- 編輯判準：原版歷史結果與本輪靜態核對分開；八個失敗案例的因果及限制保留；三語沿用相同來源、信心等級、例子與停止條件。合成例子沒有冒稱真實 run 或 CONFORMED 候選。
- 檢查：`tools/check_handbook.py` 在 Docker 唯讀執行，33 頁、192 個本機連結／錨點、三語來源／狀態詞與每語九個檢查點全部通過。錯誤飽和值、缺結果碼、非法要求誤報成功三種記憶體反例均被拒絕。
- 解析：既有 Pandoc 3.10 容器將 33 頁逐一以 GFM 解析並轉成 HTML，警告即失敗，全部通過；未產生暫存檔，也未聲稱跨瀏覽器視覺驗收。
- 來源：二十筆文件 SHA-256 重新對指定完整提交驗證，全部一致。這仍不是原版行為重跑。
- 修正：更正中文原稿少數簡體用字及日文殘留的中文術語；Docker image inspect 的入口欄位在 Python image 不存在，改查所需 image ID 後正常，屬環境查詢格式問題。
- 邊界：手冊完成；正式 schema、`osa`、真實介接器與獨立新代理／真人讀者試驗沒有執行，也不列為本次手冊已驗證成果。
- 清理：本輪容器均設 `--rm`、資源限制與外層逾時；原始資料唯讀、目前 UID/GID 執行。`osa.task=handbook` 的執行中及已停止容器均為零；沒有新建 image 或 root-owned 產物。
- 提交與推送：驗收後以獨立手冊提交推送私人 `main`；精確提交與遠端回讀結果在下一筆記錄。
